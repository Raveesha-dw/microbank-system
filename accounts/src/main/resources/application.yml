#spring.application.name=accounts

server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  config:
    import:
      - "application_qa.yml"
      - "application_prod.yml"
  profiles:
    active:
      - "qa"

#yml is very intuitive and easily readable, same file can be used in docker, kubernetes and other cloud platforms.

build:
  version: "3.0.0"

accounts:
  message: "Welcome to EliteInvest Accounts related local APIs"
  contactDetails:
    name: "Raveesha Weerasinghe"
    email: "raveesha.weerasinghe22@gmail.com"
  onCallSupport:
    - (555) 568-2348
    - (555) 568-2349